<%= render 'head' %>
<%= render 'nav' %>

<div class="container body">
  <div class="row">
    <div class="col-sm-12">
      <%= render 'messages' %>
      <%= yield %>
    </div>
  </div>
</div> <!-- /container -->

<%= render 'google_analytics' if Rails.env.production? %>
<%= render 'foot' %>
