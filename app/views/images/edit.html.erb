<%= page_title t '.title' %>

<%= simple_form_for @image,
      html: { class: 'form-horizontal' } do |f| %>

  <%= f.error_notification %>

  <%= f.input :title, autofocus: true %>

  <%= form_row t('images.image') do %>
    <%= render @image %>
  <% end %>

  <%= submit_button f: f %>

<% end %>