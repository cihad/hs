<%= render @node %>
<%= render @node.comments.order(:created_at) %>

<% if policy(Comment).create? %>
  <%= render 'comments/form' %>
<% else %>
  <%= render 'comments/user_not_signed_in' %>
<% end %>