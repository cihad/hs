<% render 'home_page_things' %>

<div class="panel panel-default">
  <div class="table-responsive">
    <table class="table">
      <tr>
        <th><%= t 'common.title' %></th>
        <th><%= t 'comments.comment_count' %></th>
        <th><%= t 'common.user' %></th>
        <th><%= t 'common.created_at' %></th>

      </tr>
      <%= render @nodes %>
    </table>
  </div>
</div>
